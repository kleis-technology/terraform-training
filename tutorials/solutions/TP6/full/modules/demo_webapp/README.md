# Webapp demonstration

This folder contains example [Terraform](https://www.terraform.io/) configuration that defines a demonstration "web application" with its infrastructure (instance or cluster).

This module imports two external modules and relies on two other example modules.

The module expects as input variables
* network information
* information about the instance to run

